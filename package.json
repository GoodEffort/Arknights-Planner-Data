{
  "name": "ak-planner-data",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "module": "commonjs",
  "moduleResolution": "node",
  "preserveConstEnums": true,
  "strict": true,
  "scripts": {
    "build": "tsc --project tsconfig.json && tsc-alias -p tsconfig.jsontsc --project tsconfig.json && tsc-alias -p tsconfig.json",
    "build:watch": "tsc && (concurrently \"tsc -w\" \"tsc-alias -w\")",
    "start": "node dist/index.js"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.14.12",
    "tsc-alias": "^1.8.10",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
